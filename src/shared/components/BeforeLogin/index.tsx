import React from 'react'

const BeforeLogin: React.FC = () => {
  return (
    <div style={{ marginBottom: 'calc(var(--base) * 0.75)' }}>
      <p style={{ margin: 0, color: 'var(--theme-elevation-500)', fontSize: '14.5px', lineHeight: 1.5 }}>
        Sign in to write, review, and publish.
      </p>
    </div>
  )
}

export default BeforeLogin
